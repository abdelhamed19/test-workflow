name: Test First Workflow

on: 
  push:
    branches: [main]


jobs:
  test-first-job:
    runs-on: ubuntu-latest

    steps:
    - name: Print Welcome Message
      run : echo "Welcome from first workflow."
      
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: List Repo Files
      run: ls -la
